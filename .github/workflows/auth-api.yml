name: Auth

on:
  push:
    branches: [main]
    paths:
      - services/auth-api/**/*.go
      - services/auth-api/Dockerfile
      - services/auth-api/sql/**
      - services/auth-api/sqlc.yaml
      - pkg/**/*.go
      - go.mod
      - go.sum
      - .github/workflows/auth-api.yml

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: services/auth-api/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/terenceodonoghue/auth-api:latest
